configfile: "dim2/tests.yaml"

rule all:
    input:
        trace_dirs = expand("serial/dim2/{test_name}/traces",
        test_name=config["tests"],
        )


# for test_name in config["tests"]:
for test_name in ["HomogeneousElasticDomain"]:
    alias = f'{test_name}_'

    module test_name:
        snakefile: f"serial/dim2/{test_name}/Snakefile"

    use rule * from test_name as alias*
